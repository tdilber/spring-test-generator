@Test
@Transactional
// TODO authorization
public void [methodName]() throws Exception {
        /* [warningMessage] */
[sets]


    MvcResult mvcResult =

        restMockMvc.perform([requestType]("[requestRoute]")
[params]
[headers]
[requestBody]
        .accept(MediaType.APPLICATION_JSON))
        .andExpect(status().isOk())
        .andExpect(content().contentType(MediaType.APPLICATION_JSON_VALUE)).andReturn();

        // Validate the in the database
        // List<entity> entityVariableList = repositoryVariable.findAll();

[asserts]
        }
